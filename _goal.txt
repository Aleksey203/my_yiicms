Цель: создать cms удобную для простого и быстрого создания сайтов(визиток, каталогов,
интернет-магазинов, новостных, порталов и пр. и особенно для более сложных работ и с большим объёмом работ по бэкенду, т.к. это конёк Yii)
При этом чтоб по мере выполнения заказов была возможность обрастать функционалом и модулями.

Основа архитектуры Димина цмс: лёгкая настройка модулей в админке, путём задания основных параметров
    (в списке: колонки , фильтры, сортировка, правила удаления, удаление депендов, запрос на смежные таблицы)
    (в форме: поля, которые генерируются gii из БД, для селектов удобное формирование списка, оформление как в eximus, загрузка файлов+ресайз, возможность вкладок)
При генерации модели, по умолчанию предлагается сгенерировать для данной таблицы модуль в админке.
Возможно списки в админке будут генерироваться при помощи CGridView

И нужно настроить генерацию модулей (из контроллеров и видов фронтенда) - это будет аналог модулей из Диминой cms и папки includes + css по имени контроллера

Из YII хотелось бы использовать его функции и библиотеку(в работе с изображениями, рендеринг, кэширование и т.д.), генератор кода, дебагер, подсветку кода + YiiStorm,
валидацию данных в модели, урл-генератор, использовать расширения(tinymce, yii-user, rights, giix, image, mailer, jui, phpexcel, importcsv, fancybox, groupgridview,
 yii-debug-toolbar, yiidebugtb, bootstrap, nestedsetbehavior, simpletree)
 использовать функции для безопасности приложения, использовать ActiveRecord, генератор форм и их валидация, CMaskedTextField, "Удобная разработка и отладка в Yii",
 переопределение классов за счёт их вложенности,
Использовать форум YII

В eximus хотелось бы взять дизайн и вёрстку админки за основу
У Саши интересное разделение файлов в начале приложения, конфигурирование и определение констант, с переменными

Нужно написать в админке модули: "Карта сайта", "Новости", "Статьи", "Пользователи(Статусы пользователей)", "Словарь", "Дизайн", "Логи", "Бэкап", "Каталог(Товары, Брэнды, Категории)",
 "Магазин(Заказы, Статусы заказов, Доставка, Статистика)"
 В админке сделать заготовки для полей ввода(текстовый инпут, текстовый textarea, текстовый tinymce, селект, чекбокс, может быть мультичекбокс, файл mysql, файлы simple)
 Нужно написать виджеты для фронтенда(Верхнее меню, Нижнее меню, Блок корзины в шапке, Последние новости, Фильтр подбора товаров)
 Написать контроллер и виды для фронтенда "Карта сайта", "Новости", "Статьи", "Каталог(Товары, Брэнды, Категории)", "Корзина"

Критерии:
- простота
- ничего лишнего
- по умолчанию минимум модулей и функций
- лёгкое подключение нужных модулей
- возможность простого подключения свёрстанных макетов
- возможость обучать других людей
- надёжность, безопасность приложений
- если того требует проект, скорость при высокой нагрузке
- простой, но красивый интерфейс админки и опять же, ничего лишнего
- возможность поставить простые сайты на конвеер,
 либо быстрота в разработке больших проектов(тогда контроль версий внедрить)
- удобный генератор кода, с разными шаблонами и опциями
- удобная и быстрая отладка приложений и выявление ошибок